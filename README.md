# basic-nlp-search-engine
This project presents a basic search engine for nlp with an app deployed on streamlit for different information retrieval techniques. It uses a dataset of nlp articles and a set of predefined search queries, returning corresponding articles for each query. The user can also chose a specific information retrieval technique (BM25,KeyBERT or RRF) 
